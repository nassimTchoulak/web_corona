import Login from "../../src/agent/Login";
import React from "react";
import {shallow} from 'enzyme';


describe('Agent componenets',()=>{


    const log = shallow(<Login/>)

        log = shallow(<Login />);

    it('image _loaded',()=>{
        expect(log.find(""))

    })



})